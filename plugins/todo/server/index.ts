// Todo plugin server-side registration
// For MVP, we don't need server-side hooks yet since we're using ProseMirror nodes
// Commenting out plugin registration until we have actual server hooks to register

// import { PluginManager } from "@server/utils/PluginManager";
// import config from "../plugin.json";

// PluginManager.add([
//   // Server hooks will be added as we implement features like templates, etc.
// ]);
