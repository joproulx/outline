import * as React from "react";
import Text from "~/components/Text";

export default function TodoSettings() {
  return (
    <div>
      <Text type="secondary">
        Todo plugin settings will be available in future phases. Currently,
        todos are enabled by default for all team members.
      </Text>
    </div>
  );
}
